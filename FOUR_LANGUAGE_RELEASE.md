# WestoryVisa 四语言前端：独立分支发布候选

分支：`work/four-language-zh-default-20260920`

## 当前边界

这是**只同步、不激活**的前端发布候选。服务器上的新目录与当前生产部署分离，
不替换 `/opt/docflow`，不重建或重启正在运行的容器，不迁移数据库，不修改域名路由。
正式站点继续运行原版本；仅把新分支推送到 GitHub 不代表已经上线。

中文保留并作为首次访问的默认语言。选择器显示 `中文`、`Español`、`Português`、
`English`，语言参数分别为 `zh-CN`、`es`、`pt-BR`、`en`。
显示语言与客户国籍、机构服务国家分开。明确选择和保存的语言优先于默认语言。

## 生产基线与恢复资料

- 原仓库基线：`85712eb7d9e62d088644a0cd0396ba82b7c31e3a`。
- 本分支的 `bc351c1` 将 `frontend/` 对齐到已保存的生产公开文件；不是完整后端快照。
- `production-snapshots/2026-09-20/public-frontend/` 保存最初的 39 份公开前端文件。
- `production-snapshots/2026-09-20/supplemental-frontend/` 补充运行容器中与公开站点对应的
  插件页、插件下载包、表单词典及 Nginx 默认页。未复制 macOS `._*` 元数据文件。
- 核对时生产前端镜像为 `westoryvisa-frontend:navigation-20260914`，
  镜像 ID 为 `sha256:35867916cd8f9e26a5de8775375b1b74821109ab38f3e37e0c4e373cf3c87526`。
  该镜像及运行容器均不在本次同步中替换或删除。

## 重要：不要部署仓库里的旧后端

线上后端是 API revision 22，本仓库历史后端源代码尚未完全对齐。
**不要以本分支根目录的 Compose 配置重建整套生产系统。**
本次只交付 `frontend/`；工作台仍要求 API revision 22，未携带本地演示 API 20 的兼容开关、
本地账户、数据库、服务器密钥或 `.env`。

以后如确认切换生产，应单独发布前端，并保留正在使用的 Nginx/Caddy 配置、API 代理、
后端镜像和数据卷；不能用仓库里的旧 Nginx 示例替代当前含浏览器会话鉴权的生产配置。
同步目录本身不提供公开预览地址，不改变线上用户流量。

## 验证

见 `tests/frontend/README.md`。测试覆盖四种语言的核心公开页、语言记忆/URL 传递、
语言切换往返以及模拟工作台状态。模拟测试不是生产客户数据、支付、OCR 或自动填写服务
的端到端验证，也不代表每一张旧宣传截图的嵌入文字均已翻译。

服务器发布目录包含提交编号和 SHA-256 文件清单，可用于核实上传文件与该分支一致。
