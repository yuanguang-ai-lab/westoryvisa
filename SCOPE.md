# 采集范围与明确缺口

主采集：2026-09-19 20:39:27–20:39:33 UTC（北京时间 09-20 04:39）。

主原始包 SHA-256：
`433aa32a36aa938ee04fca4e5b3482884526a63526b5596b09e730af9ba10aeb`

归档记录的是容器文件系统中的字节，并结合了镜像身份、实际启动入口和运行状态检查。
未冻结服务或读取进程内存，因此不把文件快照等同于所有进程已经加载的内存版本。

## 保留与去重

- 前端静态内容、生效的 Nginx 配置、插件分发 ZIP 与解包源码。
- 三种后端版本分别保留；API 中已有的新组织隔离逻辑不得被旧 `main` 回退。
- 普通 Agent 的第二实例经过逐文件相等核验后映射到同一目录。
- 107 个 `build/lib` 文件经核验与对应源码逐字节相同，归类树不再重复存放。
- 114 个 macOS AppleDouble 附属文件与 27 个自动生成的 egg-info 文件不进入归类树。
- 10 个主采集包中的宿主机运行配置文件留在私有交付，不公开原始内部运行清单。
- Chrome 专用 Dockerfile、seccomp、两份启动脚本、dockerignore 作为宿主机构建候选单列；启动脚本字节与运行容器一致。

## 仍不是可复现发布

- 源码不能映射为现有 Git 历史中的单个完整部署提交；镜像标签不是 Git 提交号。
- 宿主机构建文件可能不是镜像历史构建时的同一版本；未取得带源码提交签名的构建证明。
- 基础镜像、系统包和浏览器安装未全部使用不可变版本。现场观察 Chrome 152.0.7977.75、Nginx 1.27.5、PostgreSQL 17.11，但仅记录版本不等于保存了可回滚镜像。
- Python 安装包版本清单不包含完整 wheel/hash 锁、系统依赖或浏览器二进制。
- 数据库结构文件仅为宿主机源码候选。未读取业务数据库核对实际迁移状态；不能用这些 SQL 推定实际数据结构或直接执行迁移。
- 预期的试用次数迁移与辅助脚本未在所查宿主机路径找到，原始排除清单保留这一事实。
- 测试目录可能含合成凭据/客户样例，审核时按测试上下文确认；启发式扫描无真实密钥发现，不等于数学意义上的无敏感内容证明。
- 仅执行文件哈希、归档结构、静态语法及源码审核。没有在线调用破坏性接口、执行真实签证任务、安装插件或创建测试客户。

## 后续恢复发布能力的必要工作

1. 在隔离环境重建各组件，验证运行入口、依赖和健康检查。
2. 补齐迁移版本，保存并验证可拉取的镜像 digest / 回滚镜像；记录数据库备份及恢复演练结果。
3. 建立不会重新使用旧配置覆盖 Chrome 路由的统一部署清单。
4. 先审查差异并跑测试，再有意识地统一各 Worker 的代码；不要在本次快照中消灭真实差异。
5. 将安全与业务缺陷分别修复、评审、测试，批准后才部署。
