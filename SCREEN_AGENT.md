# DocFlow Computer Use 逐页填写演示

当前版本使用 Codex Desktop 的 Computer Use 操作用户可见的 Chrome 窗口，不再依赖 DocFlow Chrome 扩展，也不使用 DOM 注入、Playwright、Selenium 或第三方 RPA。

DocFlow 网站负责整理当前客户的字段计划、创建 60 分钟短时任务并显示进度；Codex Computer Use 负责逐项点击、输入、选择和复读。网页不能静默启动 Codex，因此交接时会把一次性启动指令复制到剪贴板，用户回到 Codex 发送后才开始操作。

## 首次准备

1. 安装并登录 Codex Desktop。
2. 在 Codex 中启用官方 Computer Use 能力。
3. 使用带有 Computer Use 能力的 Codex Desktop。网站复制的启动指令已包含任务读取、校验与状态更新规则；本项目的 `docflow-ds160` 本地 Skill 仅用于复用这些规则，不是 Chrome 扩展。
4. 通过 `启动完整版本.command` 启动 DocFlow，不要直接双击 `index.html`。

不再需要打开 `chrome://extensions`，也不需要安装 `docflow-chrome-extension`。该目录仅作为历史版本保留。

## 每次填写

1. 登录 DocFlow，打开正确的机构账号和客户档案。
2. 在“Computer Use 逐页填写”中确认前置资料已齐全。
3. 点击“准备任务并打开 CEAC”。DocFlow 会在 Google Chrome 中打开 CEAC 起始页。
4. 顾问人工完成申请地点、验证码、找回问题或会话恢复，进入第一张正式表格。
5. 回到 DocFlow，点击“我已进入表格，交给 Computer Use”。
6. 回到 Codex，将已复制的启动指令发送出去。
7. Computer Use 每次只做一个可见操作，重新读取页面并复读字段；Yes/No、下拉框等触发动态分支后会等待页面稳定再继续。
8. 普通页面只有在全部计划字段验证成功且没有可见必填报错时，才按任务设置点击 Next。遇到敏感或未映射页面会暂停。

## 稳健节奏

- 普通操作间隔：900–1500 ms。
- 动态选择后等待：至少 2000 ms。
- 页面跳转后等待：至少 2800 ms。
- 每做一个操作就重新读取一次可访问性树。
- 每个失败动作最多重试一次，避免重复写入或死循环。

这些等待用于处理 CEAC 的动态刷新和可见校验，不用于伪装人工行为或规避网站检测。

## 安全边界

- 只操作用户当前可见、已授权的官方页面。
- 一次性令牌只保存在当前 DocFlow 页面内存和剪贴板中，不写入客户数据库。
- 页面刷新后需重新准备任务；旧任务会被撤销并擦除字段值。
- 不处理验证码、账号密码、拒签/逾期/犯罪/安全背景判断、电子签名、法律声明、付款、选位和最终提交。
- 不读取无关标签页、Cookie、浏览器存储、密码或扩展。
- 页面控件不唯一、字段复读失败或出现新必填项时，Computer Use 会停止并交还人工。

## 当前覆盖

字段计划覆盖个人信息、地址电话、护照、旅行、同行人、以往赴美、美国联系人、家庭、工作教育和 F/J 的 SEVIS 信息。只有当前档案中已经收集、能明确映射且不属于人工判断边界的字段会进入任务。

DS-160 会根据签证类别和答案显示不同分支。Computer Use 在每次选择后重新读取页面，避免继续使用已经失效的控件索引；未提供或无法唯一匹配的字段不会被猜测。
