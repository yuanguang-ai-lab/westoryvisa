# 后续改动与发布约定

## 一次改动一个分支

以 `work/reconcile-production-20260920` 的已核验基线为出发点，使用 `fix/简短主题` 或 `feature/简短主题`。
开发者应先确认修改的是哪个 `components/` 目录，以及是否需要同时升级其他组件。
不要把普通 Agent 与 Chrome Agent、API 与 OCR 的差异未经核对就统一。

提交或 PR 至少写清：

1. 基线提交、涉及组件、修改目的。
2. 测试命令、结果和没有验证的内容。
3. 是否修改数据库、环境配置、浏览器/系统依赖。
4. 发布顺序、回滚目标和观察项。

第一阶段的整改 PR 先面向工作分支。完成重建、迁移和安全验证后，再计划如何合回 `main`。
本次没有修改现有 PR #1、分支保护规则或生产部署。

## 生产归档只作参照

以归档提交和标签定位源码，不再使用“服务器上最新那份”作为版本标识。
新版本应生成自己的发布清单，不改写 `PROVENANCE.json` 中本次采集的来源事实。
原始固定提交的哈希清单可用 `python3 tools/verify_snapshot.py` 检查。

## 真正发布前

- 所有相关测试通过，新增针对已发现问题的回归测试。
- 每个镜像标签关联 commit SHA，部署记录保存 image digest、迁移版本、时间及操作者。
- 确认客户数据备份可恢复、回滚镜像可取得；源码快照不是数据备份。
- 先部署测试环境，确认无误后再经过单独批准部署生产。
- 禁止直接用旧 `main` 重建整个服务、把所有 Worker 换成某一个现有镜像，或在生产上运行源码中的 smoke 脚本。
- `.env`、访问令牌、客户文件、日志、浏览器配置留在受控运行环境，不提交 Git。

完成这些前，本分支只是已归类的源代码起点，不承诺 `docker compose up` 可以原样恢复线上。
