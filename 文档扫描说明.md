# DocFlow 文档扫描说明

## 选用方案

当前版本支持两种可切换的解析 Provider，并共用后续 DS-160 映射层：

1. **MinerU 精准解析（推荐）**：后端使用签名地址上传文件，调用 `vlm` 模型并异步轮询结果，读取 `full.md` 和 `content_list.json`。适合复杂版面、表格、扫描件和多栏文档。
2. **Docling Serve + RapidOCR（本地降级）**：材料只在本机解析；保留现有图片旋转和文字质量门控。
3. **DS-160 映射层**：两种 Provider 都转换为同一结果结构，再把护照、身份证、I-20 / DS-2019、行程、酒店、邀请函和在职证明映射到核查字段。
4. **一致性与复核分流**：对格式、置信度和多材料结果进行校验。普通字段通过校验后标记为“系统已校验”，关键、冲突和低置信度字段进入顾问重点复核。

MinerU 是云端服务。启用前应确认机构的数据处理政策、客户授权、数据跨境和留存要求。Token 只保存在后端 `.env`，不会返回浏览器；未配置 Token 时 `auto` 模式不会把证件上传给 MinerU。

如果客户确认表中明确勾选或写出 Yes / No，系统可以提取该明确答案并保留原文证据；没有出现答案时不会默认推断为 No。安全与背景、拒签、逾期、犯罪、移民违规等内容仍需顾问最终确认。

## 第一次安装

在 Finder 中打开项目文件夹，依次操作：

1. 从 MinerU API 管理页创建 Token。
2. 复制 `.env.example` 为 `.env`，填写 `MINERU_API_TOKEN`。
3. 双击 `启动完整版本.command`，等待终端显示前端和后端地址。

使用 MinerU 时不需要安装本地 OCR 模型。若未配置 MinerU，脚本仍会自动安装并启动 Docling + RapidOCR。

安装脚本会自动选择 Python 3.10 至 3.13，并优先使用 3.13、3.12、3.11。不要使用 Python 3.14：当前依赖链中的 `grpcio` 可能没有对应的 macOS 预编译 wheel，从而触发 clang 本地编译失败。如果旧环境使用 3.14，脚本会自动用兼容版本重建。

## 日常启动

以后只需双击：

```txt
启动完整版本.command
```

它会同时启动独立的前端和后端：

- DocFlow 前端：`http://127.0.0.1:4175`
- DocFlow 后端：`http://127.0.0.1:4176/api`
- 选择本地 Provider 时，另外启动 Docling：`http://127.0.0.1:5001`

如果 4175 已被占用，脚本会自动选择下一个可用端口，并在浏览器打开正确地址。

## 扫描后的数据存在哪里

- 原始上传文件：`data/uploads/<机构>/<客户档案>/`
- 本机扫描服务密钥：`data/docling_api_key`（仅当前 macOS 用户可读）
- 识别文本和结构化解析 JSON：SQLite 的 `documents` 表
- DS-160 映射字段：`ds160_fields` 表
- 字段来源、页码和识别证据：`field_evidence` 表
- 条件分支答案与人工核查状态：`ds160_answers` 表
- 冲突、缺失和低置信度提醒：`review_issues` 表

网页不会展示服务器文件路径。上传文件也不会通过静态网址直接公开。
扫描服务只监听 `127.0.0.1`，并由本机随机 API Key 保护。
资料页顶部的“客户材料库”会集中显示当前客户的所有原件。点击“查看原件”时，服务器会再次核对登录账号和机构归属，不允许跨机构读取。
机构工作台的每个客户卡片也提供“客户文档库”入口，后续需要复用材料时不必重新上传。

## 当前支持的自动映射

- 中国居民身份证：姓名、身份证号码、出生日期、性别和证件地址；支持号码被 OCR 拆成空格、点号或换行，并对 18 位号码执行校验位检查
- 中国普通护照及其他 TD3 护照：英文姓名、出生日期、性别、国籍、护照号、护照类型、签发日期、签发地和有效期；支持中英双语标签、MRZ 空格与常见数字字符混淆
- I-20 / DS-2019：SEVIS ID、学校名称、学校地址、项目或专业
- DS-2019 / DS-7002：Program Number、Sponsor、项目资料
- 行程 / 酒店 / 邀请函：抵离日期、航班、城市、访问地点、美国地址和联系人
- 在职证明：雇主、地址、电话、入职日期、职位、收入和职责
- 过往美国签证：签发日期、签证号码和签证类别

识别结果会先经过格式和一致性校验。顾问重点核查关键身份字段、签证类别专属字段、低置信度结果和跨材料冲突，无需逐项点击已经通过系统校验的普通字段。
条件问答规则及安全边界见 `DS160_RULES.md`。

## 中文转英文与顾问已知信息

资料页的“顾问已知信息”可以粘贴微信、邮件或工作笔记中的连续文字。系统先用确定性规则提取证件号、日期、电话、邮箱和地址等高确定性内容，再由本机语义模型从原文证据中补充姓名、学校、雇主、旅行与说明类字段，最后统一写入字段核查。

- 不要求客户资料严格写成“字段名：值”；一整段聊天记录式文字也会按 DS-160 字段拆分。
- 语义模型返回的每一项都必须带有原文证据。证据不在粘贴文字中的结果会被服务器拒绝，不会凭空补全资料。
- 一次整理会同时完成字段识别、中文转英文、日期和停留时间标准化、地址拆分与重复字段合并。
- 页面会显示规则识别、词典转换、本地语义补漏等来源，并保留中文原文供字段核查。

- 中文原文会保留在客户档案中，英文值单独保存，不会覆盖原文。
- 所有准备写入 CEAC 的字段都会转换为英文或罗马字母，包括护照拼音姓名、城市、省份、学校、公司、地址、职位、职责和说明文字。唯一例外是 DS-160 的 `Full Name in Native Alphabet`，该字段按表格要求保留中文。
- OCR 原始文字和字段证据仍保留材料原文，便于追溯；字段核查页与 Browser Agent 使用的是转换后的英文值。
- 中国地址会拆成省份、最本地一级城市、区县、街道/乡镇、道路、小区、楼栋、单元、楼层、房间和邮编。地名、小区名只做拼音转写，不再交给通用翻译模型改写含义；例如“花好悦园”会保留为 `HUAHAOYUEYUAN`，不会被翻译成一句英文。已有档案中由旧地址解析器生成、尚未人工确认的字段，会从保留的中文原文自动重新整理，并在需要时补出地址第 2 行。
- 客户在补充问卷的文字框中输入单独一个 `D`，保存时会自动变成 `DOES NOT APPLY`。
- 常见国家、地名、学校 / 公司后缀、职业和 DS-160 常用表达使用本地规则直接转换；自由文本优先交给本机 Ollama 翻译。
- 本机翻译不可用时，系统会保留中文原文并生成拼音转写，同时把该英文值标记为待顾问核对，不会静默把不确定内容当成已确认翻译。
- 学校、公司和机构名称优先采用材料中已有的官方英文名。本地生成的机构英文名会保留中文原文和翻译来源，避免把推测名称伪装成官方名称。
- Browser Agent 发出动作前还会执行一次 CEAC 字符检查。除母语姓名外，字段只允许 `A-Z`、数字、空格及 CEAC 接受的少量标点；仍含中文的动作会暂停，不会点击下一页。
- 旧档案会在新版后端启动时自动升级语言字段，无需重新上传材料。以后启用本地语义模型时，也可根据保留的中文原文重新生成更好的英文值。

如需启用自由文本的本机语义翻译，先在本机安装并启动 Ollama，再准备一个支持中英文的模型。默认配置为：

最简单的方式是双击项目中的 `启用本地英文翻译.command`。脚本只连接本机 Ollama，不会把客户资料发送到公共翻译网站；首次运行会引导安装 Ollama 并下载免费模型。完成后重新运行 `启动完整版本.command`。

完成一次启用后，“启动完整版本.command”会在以后启动网站时自动检查并拉起本机 Ollama。模型没有安装或暂时不可用不会阻塞 DocFlow，页面会降级为增强规则识别并明确显示当前模式。

```env
DS160_TRANSLATION_PROVIDER=auto
DS160_TEXT_ANALYSIS_PROVIDER=auto
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:3b
```

模型名称可以按本机已有模型修改。所有翻译请求只从 DocFlow 服务器发送到 `127.0.0.1`，浏览器端不会接触模型配置或密钥。设置 `DS160_TRANSLATION_PROVIDER=off` 可停用本机模型，只保留规则与拼音转写。

F1 客户的上传页不会显示“在职证明”槽位；B1/B2 和 J1 是否上传该材料由中介根据客户实际情况决定。系统先从已上传材料中提取信息，再把没有覆盖的固定字段和条件问题整理为客户专属补充链接。客户提交后，数据直接回流到该客户档案；材料已经提供的字段不会重复出现在补充表中。

## 上传与删除

页面仅在“邀请函”“过往美国签证”“其他支持材料”旁显示“选传”。中介仍可根据客户实际情况决定上传哪些资料。文件上传错误时，点击材料右上角的删除图标并确认即可。系统会同时清理：

- 本机保存的原始文件
- OCR 识别文字和 Docling JSON
- 由该文件产生的 DS-160 映射字段
- 字段来源、页码和证据记录

扫描完成后，可点击“查看识别文字与映射字段”核对当前 Provider 的实际结果。识别出文字但没有可靠字段时，系统会明确显示 0 个映射字段，不会用猜测值填充初稿。

如果系统选择了旋转后的结果，材料状态会显示“自动旋转 90° / 180° / 270°”。角度表示扫描时采用的校正方向，原始上传文件不会被覆盖或改写。

## 关于字节招聘的简历解析能力

字节跳动招聘产品使用的具体简历解析服务没有公开可直接安装的开源实现或通用接口，不能把其产品表现等同于一个可接入的公开 API。成熟简历解析的准确率通常来自完整链路，而不只是 OCR：

1. 原生 PDF / Office 文本读取
2. 版面、表格和阅读顺序分析
3. 扫描页 OCR 与图像质量处理
4. 姓名、日期、机构、经历等关键信息抽取
5. 字段标准化、多来源一致性校验和人工反馈数据

后续可用真实、脱敏的 DS-160 客户材料建立评测集，再比较以下公开方案：

- **PaddleOCR PP-StructureV3 / PP-ChatOCRv4**：公开文档解析和关键信息抽取方案，适合继续做本地或私有化部署。
- **MinerU**：本项目已接入精准解析 API，默认使用 `vlm`，输出结构化 Markdown / JSON；后续应使用脱敏评测集与本地方案做同批对照。
- **火山引擎 OCR / 文档解析**：云端商业 API，接入前需要预算、数据出境与客户隐私评估，不能在未获得机构授权时直接上传护照等敏感材料。

Docling + RapidOCR 的优势是本地、可控；MinerU 的优势是复杂文档结构恢复能力。是否长期使用 MinerU，应以同一批脱敏材料上的字段准确率、单页耗时、失败率、成本和合规要求为准。

## 常见问题

### 页面显示“解析服务尚未配置”

MinerU 模式下检查后端 `.env` 是否包含 `OCR_PROVIDER=mineru` 和有效的 `MINERU_API_TOKEN`，然后重启后端。Token 不能放在 `frontend/runtime-config.js` 或任何浏览器代码中。

### 页面显示“本地扫描服务尚未启动”

只要已经上传至少一份材料，“开始扫描并生成初稿”就可以点击。页面会先自动启动本机 Docling Serve 与 RapidOCR，再提交扫描任务；也可以点击资料页状态栏中的“启动扫描服务”。首次加载通常需要 10 至 30 秒。

如果自动启动失败：

1. 关闭当前终端窗口
2. 重新双击 `启动完整版本.command`
3. 等资料页显示“服务可用”后再次点击扫描
4. 仍然失败时查看 `data/docling-serve.log` 中的错误信息

网页服务器修改后需要重新启动才会加载新的 `/api/ocr/start` 接口。只刷新浏览器不会重启 Python 服务器。

### 文件扫描完成但没有字段

OCR 可能已经读出文本，但当前规则没有找到可靠标签。该材料仍会显示“扫描完成”，字段不会被猜测填写。先在“查看识别结果”中判断是 OCR 没读出文字，还是文字已读出但字段映射遗漏。当前护照日期映射已支持中英文标签、换行和 OCR 杂字符。仍未识别到的内容会进入客户补充链接，不要求中介逐项代填；后续应使用脱敏真实样本继续扩展映射规则。

### 能否直接连接真实 DS-160 网站

当前版本不会连接或提交真实 DS-160。应先通过真实材料样本验证识别准确率、字段映射、人工复核和审计记录，再单独评估浏览器自动化及其合规要求。
