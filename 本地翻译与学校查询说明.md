# 本地翻译与学校查询

## 启用 LibreTranslate

1. 双击 `安装LibreTranslate.command`。
2. 脚本优先复用文档扫描环境自带的 Python 3.11 创建独立翻译环境；若不存在才下载 Python 3.12。它不会使用容易编译失败的系统 Python 3.14。
3. 首次安装会下载 LibreTranslate、Argos Translate 和中英模型，需要等待一段时间。
4. 安装成功后，关闭旧的 DocFlow 终端，再双击 `启动完整版本.command`。
5. 已经整理过的客户资料不会被后台静默改写。回到“收集客户资料”，再次点击“识别、翻译并整理”，即可用新翻译与学校查询能力重新生成结果。

翻译只在服务端执行。客户浏览器不会看到 LibreTranslate 地址，也不会接触任何 API Key。中文姓名仍按拼音处理；地址、学校、公司和工作说明优先翻译为正常英文语序。

## 学校地址

顾问点击“识别、翻译并整理”时，系统会对资料中出现的任意学校名称执行通用实体解析，并把结果拆分成英文校名、街道、城市、省份、邮编和国家。只会发送学校名称及已有的城市/省份提示，不会发送客户姓名、电话、护照或家庭地址。

解析顺序如下：

1. 读取本地已核验缓存。
2. 用 OpenStreetMap Nominatim 搜索教育机构，并拒绝餐厅、公司等非学校结果。
3. 用 Wikidata 核对实体并读取英文名称；学校只有坐标时，再用 Nominatim 反向查询结构化地址和邮编。
4. 公开实体没有英文名时，调用 LibreTranslate；常见“某市第 N 高级中学”可使用受控学校名称语法转换，但会标记为待核对。
5. 同名学校无法区分、结果类型不是教育机构或证据不完整时，不自动猜测，保存为“待定位/待核对”。

Nominatim 请求遵守一秒一次、缓存结果和明确 User-Agent 的要求。解析结果会保存来源链接、坐标、置信度和是否需要核对；学校名称不会再用 `SHI / DI YI / GAO JI` 这种逐字拼音冒充英文校名。

## 配置

`.env` 可使用：

```text
DS160_TRANSLATION_PROVIDER=auto
LIBRETRANSLATE_URL=http://127.0.0.1:5000
SCHOOL_LOOKUP_PROVIDER=nominatim
```

`SCHOOL_LOOKUP_PROVIDER=auto` 与 `nominatim` 都会启用完整的 Nominatim + Wikidata 解析链。可选高级配置：

```text
SCHOOL_LOOKUP_URL=https://nominatim.openstreetmap.org/search
SCHOOL_REVERSE_URL=https://nominatim.openstreetmap.org/reverse
SCHOOL_WIKIDATA_URL=https://www.wikidata.org/w/api.php
SCHOOL_LOOKUP_COUNTRYCODES=
```

`SCHOOL_LOOKUP_COUNTRYCODES` 默认留空，允许查询全球学校；若机构业务只处理中国教育经历，可填 `cn` 缩小范围。公开地图中的学校覆盖率不是 100%。学校名称过短（例如只写“第一中学”）或多地重名时，请同时保留城市/省份；系统会用地点提示缩小候选，但不会在证据不足时编造地址或邮编。

若临时关闭学校查询，将 `SCHOOL_LOOKUP_PROVIDER` 改为 `off`。若临时关闭所有模型翻译，将 `DS160_TRANSLATION_PROVIDER` 改为 `off`。

## Chrome 逐页填写升级

动态 Yes/No、下拉选项、重复记录、新出现字段和 CEAC 三段式日期定位逻辑在 Chrome 扩展 `0.9.1` 中。已经安装过扩展时，请打开 `chrome://extensions`，找到 DocFlow DS-160 并点击“重新加载”；版本仍不是 `0.9.1` 时，重新双击 `安装DocFlow Chrome扩展.command`。
