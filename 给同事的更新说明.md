# DocFlow 前后端分离更新包

这是现有 DocFlow 项目的代码更新包，不包含客户数据、账号、密钥、模型、
虚拟环境，也不包含新开发中的独立 Agent。

## 推荐更新方式

1. 先完整备份你原来能够运行的 DocFlow 项目文件夹。
2. 关闭原来的 DocFlow、Docling、LibreTranslate 等本地服务。
3. 将本更新包内的全部文件复制到原项目目录，选择“合并/替换同名源码文件”。
4. 不要删除或覆盖原项目中的以下内容：
   - `data/`
   - `.env`
   - `.venv-docling/`
   - `.venv-browser-use/`
   - `.venv-libretranslate/`
5. 双击 `启动完整版本.command`。

如果只想先验证账号、数据库和前后端页面，也可以双击
`启动数据库版.command`。

## 为什么要合并到原目录

Docling/RapidOCR 的 Python 环境位于你电脑原项目的 `.venv-docling/`，
模型通常缓存在你当前 macOS 用户目录。它们不是源码的一部分，也不能通过
复制另一台电脑的虚拟环境可靠迁移。合并到你原来可运行的目录，可以继续复用
你自己的环境、模型缓存、数据库和配置。

## 本次架构变化

- 浏览器端代码位于 `frontend/`。
- 后端 API 位于 `backend/`。
- `scripts/run_local.py` 会同时启动独立前端和独立后端。
- 原启动脚本仍然保留，日常启动方式不变。
- 根目录旧入口仍保留兼容。
- OCR 和现有 Computer Use 业务接口没有在前后端分离中更换。

## 首次更新后检查

启动后确认：

- 工作台页面能够打开；
- 注册或登录正常；
- 原有客户档案仍然存在；
- 上传材料后文档扫描服务显示可用；
- `http://127.0.0.1:4176/status` 能看到后端状态页。

如果 4175 或 4176 已被占用，启动脚本会自动选择后续可用端口，请以启动窗口
实际显示的地址为准。
