# DocFlow 注册邮箱验证

DocFlow 的注册业务只调用统一的 `sendEmail()`。通过 `.env` 中的 `MAIL_PROVIDER` 可在两种方式间切换：

- `smtp`：普通 SMTP 用户名 + SMTP 授权码。
- `microsoft_oauth2`：Microsoft Outlook / Microsoft 365，使用 STARTTLS + XOAUTH2。

验证码仍为六位数字，10 分钟有效、60 秒后可重发、同一邮箱每小时最多 5 次、最多尝试 5 次。数据库只保存验证码的 HMAC 哈希，不保存明文。

当前开发版本默认使用 `REGISTRATION_VERIFICATION=none`，注册时不要求验证码。运行 `配置邮箱验证.command` 完成邮件配置后，脚本会自动改为 `REGISTRATION_VERIFICATION=email`。需要再次临时关闭时，将该值改回 `none` 并重启服务即可。

## 最简单的配置方法

1. 双击 `配置邮箱验证.command`。
2. 选择“普通 SMTP”或“Microsoft Outlook SMTP OAuth2”。
3. 按提示填写。密码、Client Secret 和 Refresh Token 输入时不会显示。
4. 配置会写入项目根目录 `.env`，无关配置不会被覆盖。
5. 关闭旧服务，双击 `启动完整版本.command`。

`.env` 已加入 `.gitignore`，文件权限会设置为 `600`。不要把 `.env`、SMTP 授权码、Client Secret 或 Refresh Token 发给别人或提交到 GitHub。

## 在 Microsoft Entra 注册应用

1. 打开 [Microsoft Entra 管理中心](https://entra.microsoft.com/)，进入“应用注册”并新建注册。
2. Microsoft 365 单租户机构可选择“仅此组织目录中的帐户”。
3. 需要支持个人 Outlook.com、Hotmail 或 Live 邮箱时，必须选择包含“个人 Microsoft 帐户”的受支持帐户类型。
4. 在“身份验证”中添加 **Web** 回调地址：

   `http://127.0.0.1:53682/callback`

   地址、端口和路径必须完全一致。它只绑定本机，授权结束后会自动关闭。

5. 在“证书和密码”中创建 Client Secret。配置时填写的是 Secret 的 **Value**，不是 Secret ID。离开页面后通常无法再次查看 Value。
6. 在“API 权限”中添加 Office 365 Exchange Online 的委托权限 `SMTP.Send`。机构租户如果要求管理员同意，请由管理员完成授权。

授权辅助脚本还会请求 `offline_access`，用于取得 Refresh Token。OAuth2 授权码和 Refresh Token 的处理遵循 Microsoft v2.0 令牌端点。

## 获取 Refresh Token

推荐流程不需要从浏览器地址栏复制任何令牌：

1. 运行 `配置邮箱验证.command` 并选择 Microsoft OAuth2。
2. 填写发件邮箱、Tenant ID、Client ID 和 Client Secret。
3. Refresh Token 一项直接回车留空。
4. 脚本会在 `127.0.0.1:53682` 临时启动一次性回调，并自动打开 Microsoft 登录页。
5. 使用发件邮箱登录并同意权限。
6. 回调收到授权码后会在服务器端换取 Refresh Token，安全写入 `.env`；终端和网页都不会显示令牌。
7. 授权完成后本地回调立即关闭，不会成为网站的常驻接口。生产部署时不要运行或对公网开放 `microsoft_oauth_setup.py`。

也可以稍后在项目目录运行：

```sh
python3 microsoft_oauth_setup.py
```

Access Token 只短暂保存在服务器内存中，接近过期时会用 Refresh Token 自动刷新；不会写入前端或长期保存。Microsoft 返回轮换后的 Refresh Token 时，会保存到权限为 `600` 的 `data/microsoft_refresh_token`。

## 个人 Outlook 与 Microsoft 365 的差异

| 账号 | Tenant ID | SMTP 地址 | 端口与安全方式 |
|---|---|---|---|
| Outlook.com / Hotmail / Live 个人账号 | `consumers` | `smtp-mail.outlook.com` | `587` + STARTTLS |
| Microsoft 365 工作/学校账号 | 实际租户 ID | `smtp.office365.com` | `587` + STARTTLS |

`SMTP_SECURE=false` 在本项目中表示“先连接 587，再升级为 STARTTLS”，不是明文发送。

Microsoft 365 管理员可能在租户或邮箱级别禁用了 SMTP AUTH。此时即使 OAuth2 配置正确也不能通过 SMTP 发信。不要退回保存邮箱登录密码，也不要关闭多因素认证或其他账号安全设置；优先让管理员按组织策略允许该专用邮箱使用 OAuth SMTP，或改用 Microsoft Graph `sendMail` / 专业事务邮件服务。

## `.env` 配置参考

Microsoft 个人邮箱：

```sh
export MAIL_PROVIDER=microsoft_oauth2
export MAIL_FROM=sender@outlook.com
export MAIL_FROM_NAME='DocFlow DS-160'
export MICROSOFT_TENANT_ID=consumers
export MICROSOFT_CLIENT_ID=你的应用ClientID
export MICROSOFT_CLIENT_SECRET=你的ClientSecretValue
export MICROSOFT_REFRESH_TOKEN=授权脚本写入的RefreshToken
export SMTP_HOST=smtp-mail.outlook.com
export SMTP_PORT=587
export SMTP_SECURE=false
```

Microsoft 365 邮箱将 Tenant ID 和 SMTP 地址改为：

```sh
export MICROSOFT_TENANT_ID=你的租户ID
export SMTP_HOST=smtp.office365.com
```

## 启动和测试

1. 关闭以前运行的 DocFlow 终端窗口。
2. 双击 `启动完整版本.command`。
3. 打开注册页，填写一个能收信的邮箱，点击“发送验证码”。
4. 查看收件箱和垃圾邮件目录，输入六位验证码完成注册。

自动化测试全部使用 mock，不会真实发送邮件：

```sh
python3 -m unittest discover -s tests -v
```

如果你的部署已经使用 PostgreSQL，请在升级后执行一次增量迁移（不会删除原表或已有数据）：

```sh
psql "$DATABASE_URL" -f migrations/20260713_email_delivery_status.sql
```

也可以在 pgAdmin 的 Query Tool 中打开并执行 `migrations/20260713_email_delivery_status.sql`。本地 SQLite 版本会在启动时自动增加相同字段。

## 常见错误

| 错误 | 排查方法 |
|---|---|
| `AADSTS50011` | Entra 中的 Web 回调必须精确填写 `http://127.0.0.1:53682/callback`。 |
| `invalid_client` | 检查 Client ID、Tenant ID，以及填写的是 Client Secret Value 而不是 ID。 |
| `invalid_grant` | Refresh Token 已撤销、过期或账号策略改变；重新运行授权脚本。 |
| 未返回 Refresh Token | 确认授权范围包含 `offline_access`，并重新同意权限。 |
| `535` / XOAUTH2 验证失败 | 检查授权账号是否与 `MAIL_FROM` 相同，并重新授权。 |
| Microsoft 365 报 SMTP AUTH disabled | 请管理员检查租户与该邮箱的 Authenticated SMTP 策略，或使用 Graph / 事务邮件服务。 |
| 连接超时 | 检查网络、防火墙是否允许连接 SMTP 587 和 Microsoft 登录端点。 |
| 邮件进入垃圾箱 | 配置发件域名 SPF、DKIM、DMARC；个人 Outlook 发件也应控制发送频率。 |

服务端错误日志只记录提供商、错误类别或 OAuth 错误码，不记录验证码、SMTP 密码、Client Secret、Refresh Token 或完整 Access Token。

## 切换回普通 SMTP

再次双击 `配置邮箱验证.command`，选择“普通 SMTP”，填写 SMTP 地址、端口、发件邮箱、用户名和授权码。脚本会把 `MAIL_PROVIDER` 改为 `smtp`，清理 Microsoft 专用配置，同时保留 `.env` 中的其他项目配置。

普通 SMTP 示例：

```sh
export MAIL_PROVIDER=smtp
export MAIL_FROM=noreply@example.com
export SMTP_HOST=smtp.example.com
export SMTP_PORT=465
export SMTP_SECURE=true
export SMTP_USERNAME=noreply@example.com
export SMTP_PASSWORD=邮箱服务商生成的SMTP授权码
```

## 官方参考

- [Outlook.com 的 POP、IMAP 和 SMTP 设置](https://support.microsoft.com/en-us/office/pop-imap-and-smtp-settings-for-outlook-com-d088b986-291d-42b8-9564-9c414e2aa040)
- [使用 OAuth 对 IMAP、POP 或 SMTP 连接进行身份验证](https://learn.microsoft.com/en-us/exchange/client-developer/legacy-protocols/how-to-authenticate-an-imap-pop-smtp-application-by-using-oauth)
- [Microsoft 身份平台 OAuth 2.0 授权代码流](https://learn.microsoft.com/en-us/entra/identity-platform/v2-oauth2-auth-code-flow)
- [Microsoft 身份平台中的 Refresh Token](https://learn.microsoft.com/en-us/entra/identity-platform/refresh-tokens)
- [Exchange Online 客户端 SMTP 提交](https://learn.microsoft.com/en-us/exchange/clients-and-mobile-in-exchange-online/authenticated-client-smtp-submission)
- [Microsoft Graph `sendMail`](https://learn.microsoft.com/en-us/graph/api/user-sendmail?view=graph-rest-1.0)
