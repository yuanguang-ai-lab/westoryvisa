# DocFlow DS-160 项目交付包

本压缩包包含项目源码、前端页面、Chrome 扩展、测试、数据库结构与迁移、安装/启动脚本及项目说明文档。

为避免泄露客户证件、账号配置或本地密钥，并避免把可重建的依赖和缓存塞进交付包，下列运行时内容未打包：

- `.env` 及本地邮箱/服务密钥。
- `.venv-*` Python 虚拟环境。
- `data/` 中的客户数据库、上传文件、日志、任务记录、模型和缓存。
- 项目根目录的本地 SQLite 数据库及 `outputs/` 数据库导出。
- `__pycache__`、`.DS_Store`、`*.pyc` 等可重建缓存。

在 macOS 上解压后，可双击 `启动完整版本.command`。首次运行会根据项目脚本安装缺失的文档扫描环境。
