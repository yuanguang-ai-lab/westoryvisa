# DocFlow 签证预约辅助

## 能做什么

完成 DS-160 后，DocFlow 可以把已核对的客户资料整理成一次性本机任务，并在顾问明确启动后写入 `www.usvisascheduling.com` 的以下白名单页面：

- Applicant Details：姓名、出生日期、国籍、护照、DS-160 确认号、邮箱和电话。
- Visa Options：签证类别、申请地点，以及页面实际出现时的 F/J 类 SEVIS 和学校信息。

预约网站的页面结构会随国家、语言、账户状态和官方更新变化。Agent 按字段标签和控件类型匹配，不使用屏幕坐标；找不到或无法复读确认的字段会暂停，不会猜测。

## 必须由顾问完成

以下操作不会进入任务数据，也不会由扩展执行：

- 注册账号或创建家属账号。
- 输入用户名、密码、短信或邮箱验证码。
- 处理 CAPTCHA 或其他安全验证。
- 支付签证费、填写支付资料或保存收据号。
- 查询、轮询、抢占或选择预约时段。
- 点击 Save、Continue、Next、Submit 或最终预约确认。

共同预约家属可保存在客户档案中供顾问核对，但扩展不会代为添加家属。每位申请人仍应使用自己的 DS-160 确认号，并按预约系统当次要求准备可用邮箱。

## 使用步骤

1. 在 DocFlow 完成客户资料整理、字段核查和核查清单。
2. 在“预约开户”点击“打开 USTravelDocs”，在新标签页选择中国及非移民签证。
3. 创建新预约账户或登录已有账户，并人工完成密码、邮箱或短信验证码。
4. 返回 DocFlow，填写预约系统邮箱并确认账户可用。
5. 在“预约资料”填写 DS-160 确认号、联系电话和申请地点。
6. F/J 类申请人核对 SEVIS ID 和学校或项目名称。
7. 保存预约资料，确认页面显示“资料齐全”。
8. 在 `chrome://extensions` 重新加载 DocFlow Page Agent v0.7.0。
9. 点击“打开预约网站并连接”。
10. 在预约网站人工登录、完成当次验证码，并进入 Applicant Details 或 Visa Options。
11. 回到 DocFlow 点击“我已登录，开始填写当前页”。
12. Agent 写入并复读字段后会停下，由顾问检查并自行继续。
13. 进入缴费、选时间或最终确认页面时，任务自动结束并从扩展会话清除字段值。

## 数据与权限

- 预约账户状态和预约资料随当前客户档案保存，继续使用现有机构和账号隔离规则。
- 一次性任务保存在本机私有目录，使用短时访问令牌；磁盘只保存令牌哈希。
- 扩展权限仅包含本机 DocFlow、`ceac.state.gov` 和 `www.usvisascheduling.com`，没有 `<all_urls>` 权限。
- `ustraveldocs.com` 仅作为人工开户注册入口，扩展不会在该网站读取或填写账号凭据。
- 任务结束、过期、被替换或停止后，扩展会清空任务中的客户字段。

## 官方流程依据

- 美国国务院说明，提交 DS-160 只是签证申请流程的第一步；如需面谈，还必须按申请地要求预约：<https://travel.state.gov/content/travel/en/us-visas/visa-information-resources/forms/ds-160-online-nonimmigrant-visa-application/ds-160-faqs.html>
- USTravelDocs 预约指引入口：<https://www.ustraveldocs.com/>
- 预约系统 Visa Options 页面：<https://www.usvisascheduling.com/en-US/applicant_details/application_details/>
- 预约系统 Manage Dependents 页面：<https://www.usvisascheduling.com/en-US/applicant_details/application_details/additional_visa_options/drop-box-confirmation/manage_dependents/>

本功能只做资料复用和页面填写辅助，不提供法律建议，不代表美国政府，不支付费用，也不完成预约交易。
